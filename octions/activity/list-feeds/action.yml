name: List feeds
description: |
  This action implements `GET` request to `/feeds`

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true

runs:
  using: node12
  main: index.js

