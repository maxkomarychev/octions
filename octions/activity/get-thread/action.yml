name: View a single thread
description: |
  

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  thread_id:
    description: |
      thread_id parameter
    required: true

runs:
  using: node12
  main: index.js

