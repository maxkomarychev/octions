name: Unfollow a user
description: |
  This action implements `DELETE` request to `/user/following/{username}`

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  username:
    description: |
      username parameter
    required: true

runs:
  using: node12
  main: index.js

