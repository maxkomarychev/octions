name: Get the authenticated user
description: |
  This action implements `GET` request to `/user`

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true

runs:
  using: node12
  main: index.js

