name: Get a single user
description: |
  This action implements `GET` request to `/users/{username}`

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  username:
    description: |
      username parameter
    required: true

runs:
  using: node12
  main: index.js

