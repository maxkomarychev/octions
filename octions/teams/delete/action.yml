name: Delete team
description: |
  This action implements `DELETE` request to `/teams/{team_id}`

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  team_id:
    description: |
      team_id parameter
    required: true

runs:
  using: node12
  main: index.js

