name: List all codes of conduct
description: |
  This action implements `GET` request to `/codes_of_conduct`

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true

runs:
  using: node12
  main: index.js

