name: Get an individual code of conduct
description: |
  

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  key:
    description: |
      key parameter
    required: true

runs:
  using: node12
  main: index.js

