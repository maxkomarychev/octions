name: List referrers
description: |
  Get the top 10 referrers over the last 14 days.

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  owner:
    description: |
      owner parameter
    required: false
  repo:
    description: |
      repo parameter
    required: false

runs:
  using: node12
  main: index.js

