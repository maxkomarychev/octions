name: Get a single deployment
description: |
  

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  owner:
    description: |
      owner parameter
    required: false
  repo:
    description: |
      repo parameter
    required: false
  deployment_id:
    description: |
      deployment_id parameter
    required: true

runs:
  using: node12
  main: index.js

