name: List deployments
description: |
  This action implements `GET` request to `/repos/{owner}/{repo}/deployments`

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  owner:
    description: |
      owner parameter
    required: true
  repo:
    description: |
      repo parameter
    required: true
  sha:
    description: |
      The SHA recorded at creation time.
    required: true
  ref:
    description: |
      The name of the ref. This can be a branch, tag, or SHA.
    required: true
  task:
    description: |
      The name of the task for the deployment (e.g., `deploy` or `deploy:migrations`).
    required: true
  environment:
    description: |
      The name of the environment that was deployed to (e.g., `staging` or `production`).
    required: true
  per_page:
    description: |
      Results per page (max 100)
    required: true
  page:
    description: |
      Page number of the results to fetch.
    required: true

runs:
  using: node12
  main: index.js

