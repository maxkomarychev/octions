name: Delete a download
description: |
  

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  owner:
    description: |
      owner parameter
    required: true
  repo:
    description: |
      repo parameter
    required: true
  download_id:
    description: |
      download_id parameter
    required: true

runs:
  using: node12
  main: index.js

