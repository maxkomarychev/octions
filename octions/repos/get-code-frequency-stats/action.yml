name: Get the number of additions and deletions per week
description: |
  This action implements `GET` request to `/repos/{owner}/{repo}/stats/code_frequency`

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  owner:
    description: |
      owner parameter
    required: true
  repo:
    description: |
      repo parameter
    required: true

runs:
  using: node12
  main: index.js

