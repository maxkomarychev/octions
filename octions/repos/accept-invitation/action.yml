name: Accept a repository invitation
description: |
  

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  invitation_id:
    description: |
      invitation_id parameter
    required: true

runs:
  using: node12
  main: index.js

