name: Delete a release asset
description: |
  

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  owner:
    description: |
      owner parameter
    required: false
  repo:
    description: |
      repo parameter
    required: false
  asset_id:
    description: |
      asset_id parameter
    required: true

runs:
  using: node12
  main: index.js

