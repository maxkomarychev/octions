name: Delete a repository invitation
description: |
  

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  owner:
    description: |
      owner parameter
    required: false
  repo:
    description: |
      repo parameter
    required: false
  invitation_id:
    description: |
      invitation_id parameter
    required: true

runs:
  using: node12
  main: index.js

