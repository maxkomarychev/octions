name: Delete an authorization
description: |
  This action implements `DELETE` request to `/authorizations/{authorization_id}`

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  authorization_id:
    description: |
      authorization_id parameter
    required: true

runs:
  using: node12
  main: index.js

