name: Delete an authorization
description: |
  

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  authorization_id:
    description: |
      authorization_id parameter
    required: true

runs:
  using: node12
  main: index.js

