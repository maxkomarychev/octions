name: Delete a project card
description: |
  

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  card_id:
    description: |
      card_id parameter
    required: true

runs:
  using: node12
  main: index.js

