name: Get a single review
description: |
  

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  owner:
    description: |
      owner parameter
    required: false
  repo:
    description: |
      repo parameter
    required: false
  pull_number:
    description: |
      pull_number parameter
    required: true
  review_id:
    description: |
      review_id parameter
    required: true

runs:
  using: node12
  main: index.js

