name: Delete a label
description: |
  

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  owner:
    description: |
      owner parameter
    required: false
  repo:
    description: |
      repo parameter
    required: false
  name:
    description: |
      name parameter
    required: true

runs:
  using: node12
  main: index.js

