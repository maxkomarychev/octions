name: Fork a gist
description: |
  This action implements `POST` request to `/gists/{gist_id}/forks`

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  gist_id:
    description: |
      gist_id parameter
    required: true

runs:
  using: node12
  main: index.js

