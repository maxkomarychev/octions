name: Delete a gist
description: |
  This action implements `DELETE` request to `/gists/{gist_id}`

inputs:
  token:
    description: |
      Token to authenticate the request
    required: true
  gist_id:
    description: |
      gist_id parameter
    required: true

runs:
  using: node12
  main: index.js

